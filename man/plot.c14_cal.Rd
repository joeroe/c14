% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.c14_cal}
\alias{plot.c14_cal}
\title{Plot calibrated radiocarbon dates}
\usage{
\method{plot}{c14_cal}(
  x,
  ...,
  max.plot = 20,
  resolution = NULL,
  fixed_xlim = FALSE,
  show_curve = TRUE
)
}
\arguments{
\item{x}{A \link{cal} vector of calibrated radiocarbon dates.}

\item{...}{Additional arguments passed to \code{\link[=plot.default]{plot.default()}}.}

\item{max.plot}{Maximum number of plots to generate. A warning is issued if
\code{length(x) > max.plot} and only the first \code{max.plot} elements will be
plotted. Default: \code{20}.}

\item{resolution}{Resolution of the age grid in years. If \code{NULL} (default),
automatically calculated to produce approximately 500 points across the
age range for smooth curves.}

\item{fixed_xlim}{Logical. If \code{TRUE}, all plots share the same x-axis range
(calculated from all dates). If \code{FALSE} (default), each plot uses its own
age range focused on that specific date.}

\item{show_curve}{Logical. If \code{TRUE} (default), plot the calibration curve
as the main plot with the probability distribution overlaid on the right
y-axis. If \code{FALSE}, only show the probability distribution.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of generating plots.
}
\description{
Plots each element of a \link{cal} vector as a separate calendar probability
distribution. Use \code{par(mfrow)} or \code{par(mfcol)} to arrange multiple plots.
}
\examples{
x <- cal(c(1000, 2000, 3000), c(30, 40, 50), IntCal20)
plot(x)

# Arrange multiple plots
par(mfrow = c(2, 2))
plot(x)
dev.off()

# Use same fixed x axis limits for all plots
plot(x, fixed_xlim = TRUE)

# Hide calibration curve
plot(x[1], show_curve = FALSE)
}
