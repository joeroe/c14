% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cal_summary.R
\name{cal_hdr}
\alias{cal_hdr}
\alias{cal_hdi}
\title{Highest Density Region and Highest Density Interval}
\usage{
cal_hdr(x, interval = 0.954)

cal_hdi(x, interval = 0.954)
}
\arguments{
\item{x}{A \link{cal} vector of calibrated radiocarbon dates.}

\item{interval}{Numeric. The probability content of the HDR/HDI. Default:
0.954 (approximately 2 standard deviations).}
}
\value{
For \code{cal_hdr()}: A list of the same length as \code{x}. Each element is a list of
numeric vectors, where each vector has two elements: the start and end ages
of an HDR interval. For multimodal distributions, multiple intervals may be
returned.

For \code{cal_hdi()}: A list of the same length as \code{x}. Each element is a numeric
vector with two elements: the start and end ages of the HDI interval.
}
\description{
\code{cal_hdr()} calculates the Highest Density Region (HDR) for calibrated
radiocarbon dates. The HDR is the shortest interval (or set of intervals)
that contains the specified proportion of the probability mass.

\code{cal_hdi()} calculates the Highest Density Interval (HDI), which is the
single shortest contiguous interval containing the specified proportion of
the probability mass. Unlike \code{cal_hdr()}, which can return multiple intervals
for multimodal distributions, \code{cal_hdi()} always returns exactly one interval.
}
\examples{
x <- c14_calibrate(5000, 10)
cal_hdr(x)
cal_hdi(x)
cal_hdr(x, interval = 0.683)
cal_hdi(x, interval = 0.683)
}
\seealso{
Other functions for summarising calibrated radiocarbon dates:
\code{\link[=cal_age_range]{cal_age_range()}},
\code{\link[=cal_mode]{cal_mode()}},
\code{\link[=cal_probability]{cal_probability()}}
}
\concept{functions for summarising calibrated radiocarbon dates}
